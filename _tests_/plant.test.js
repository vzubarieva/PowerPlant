import {storeState,changeState} from "../src/js/plant.js";

describe("storeState", () => {
  test("should store the currantState of an object", () => 
    const newState = currentStant();
    expect(newState).toEqual(currantState);
});
